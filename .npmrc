# Force npm to install devDependencies even when NODE_ENV=production
# This is required because Replit's nodejs-20 module sets NODE_ENV=production
# but development tools like @vitejs/plugin-react are needed for the dev server
omit=optional
